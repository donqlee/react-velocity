export interface OptimizedImageProps {
  src: string;
  alt: string;
  width?: number;
  height?: number;
  lazy?: boolean;
  webp?: boolean;
  responsive?: boolean;
}

export const OptimizedImage = () => {
  return null;
};

export default OptimizedImage;
